pipeline {
    agent any
    stages {
        stage('Example') {
            	steps {
               		sh 'echo $(git log -n 1 --pretty=format:"%H")'
            }
        }
    }
    post { 
        success { 
            echo 'successul build'
        }
    }
}
